---
import ConditionDetail from '../../components/site/ConditionDetail.astro';
import { pathsFor } from '../../i18n/content';

export async function getStaticPaths() {
    return pathsFor('conditions', 'ar');
}

const { entry } = Astro.props;
---

<ConditionDetail entry={entry} />
